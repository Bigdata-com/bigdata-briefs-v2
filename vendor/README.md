# Vendor — external tools

## sentiment_tool

Used for portfolio brief company ranking.

Clone or pull the latest version here:

```bash
git clone https://gitlab.com/ravenpack/data-science/sentiment_tool.git vendor/sentiment_tool
```

Or configure `SENTIMENT_TOOL_PATH` in `.env` to point to your existing clone.
